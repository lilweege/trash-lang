i64 N; N = 10000;

i64 i; i = 0;
i64 sign; sign = 1;
f64 pi; pi = 0.;

while (i < N) {
    pi = pi + sign * (1.0 / ((2*i) + 1));
    sign = -sign;
    i = i + 1;
}
pi = pi * 4;

putf(pi);
putc('\n');
